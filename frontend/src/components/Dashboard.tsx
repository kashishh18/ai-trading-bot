import React, { useState, useEffect } from 'react';\nimport {\n  TrendingUp,\n  TrendingDown,\n  DollarSign,\n  Target,\n  Activity,\n  Zap,\n  Brain,\n  Shield,\n  Clock,\n  BarChart3,\n  PieChart,\n  AlertTriangle,\n  CheckCircle,\n  Loader\n} from 'lucide-react';\nimport {\n  LineChart,\n  Line,\n  XAxis,\n  YAxis,\n  CartesianGrid,\n  Tooltip,\n  ResponsiveContainer,\n  PieChart as RechartsPieChart,\n  Cell,\n  BarChart,\n  Bar,\n  Area,\n  AreaChart\n} from 'recharts';\n\n// Types\ninterface PortfolioData {\n  total_value: number;\n  cash_balance: number;\n  invested_amount: number;\n  unrealized_pnl: number;\n  realized_pnl: number;\n  total_return: number;\n  total_return_percent: number;\n  number_of_positions: number;\n  win_rate: number;\n  total_trades: number;\n}\n\ninterface MarketData {\n  symbol: string;\n  current_price: number;\n  percent_change: number;\n  volume: number;\n  company_name: string;\n}\n\ninterface TradingStats {\n  total_signals: number;\n  successful_trades: number;\n  failed_trades: number;\n  success_rate: number;\n  uptime_hours: number;\n}\n\ninterface Alert {\n  id: number;\n  type: string;\n  message: string;\n  symbol?: string;\n  timestamp: string;\n}\n\nconst Dashboard: React.FC = () => {\n  // State\n  const [portfolioData, setPortfolioData] = useState<PortfolioData | null>(null);\n  const [marketData, setMarketData] = useState<MarketData[]>([]);\n  const [tradingStats, setTradingStats] = useState<TradingStats | null>(null);\n  const [alerts, setAlerts] = useState<Alert[]>([]);\n  const [isLoading, setIsLoading] = useState(true);\n  const [lastUpdate, setLastUpdate] = useState<Date>(new Date());\n\n  // Mock performance data for the chart\n  const [performanceData, setPerformanceData] = useState([\n    { date: '2025-01-01', value: 100000, pnl: 0 },\n    { date: '2025-01-02', value: 101500, pnl: 1500 },\n    { date: '2025-01-03', value: 99800, pnl: -200 },\n    { date: '2025-01-04', value: 103200, pnl: 3200 },\n    { date: '2025-01-05', value: 105600, pnl: 5600 },\n    { date: '2025-01-06', value: 104900, pnl: 4900 },\n    { date: '2025-01-07', value: 107300, pnl: 7300 }\n  ]);\n\n  // Fetch dashboard data\n  const fetchDashboardData = async () => {\n    try {\n      // Fetch portfolio summary\n      const portfolioResponse = await fetch('/api/portfolio/summary');\n      if (portfolioResponse.ok) {\n        const portfolioData = await portfolioResponse.json();\n        setPortfolioData(portfolioData);\n        setTradingStats(portfolioData.engine_stats);\n      }\n\n      // Fetch trending stocks\n      const marketResponse = await fetch('/api/market/trending');\n      if (marketResponse.ok) {\n        const marketData = await marketResponse.json();\n        setMarketData(marketData.slice(0, 6)); // Top 6 trending\n      }\n\n      // Fetch recent alerts\n      const alertsResponse = await fetch('/api/alerts/recent?limit=10');\n      if (alertsResponse.ok) {\n        const alertsData = await alertsResponse.json();\n        setAlerts(alertsData);\n      }\n\n      setLastUpdate(new Date());\n      setIsLoading(false);\n    } catch (error) {\n      console.error('Error fetching dashboard data:', error);\n      setIsLoading(false);\n    }\n  };\n\n  // Initial load and refresh every 15 seconds\n  useEffect(() => {\n    fetchDashboardData();\n    const interval = setInterval(fetchDashboardData, 15000);\n    return () => clearInterval(interval);\n  }, []);\n\n  // Chart colors\n  const COLORS = ['#3B82F6', '#10B981', '#F59E0B', '#EF4444', '#8B5CF6', '#F97316'];\n\n  // Format currency\n  const formatCurrency = (amount: number) => {\n    return new Intl.NumberFormat('en-US', {\n      style: 'currency',\n      currency: 'USD',\n      minimumFractionDigits: 0,\n      maximumFractionDigits: 0\n    }).format(amount);\n  };\n\n  // Format percentage\n  const formatPercent = (percent: number) => {\n    return `${percent >= 0 ? '+' : ''}${percent.toFixed(2)}%`;\n  };\n\n  if (isLoading) {\n    return (\n      <div className=\"flex items-center justify-center h-64\">\n        <div className=\"flex items-center space-x-3 text-gray-400\">\n          <Loader className=\"w-6 h-6 animate-spin\" />\n          <span>Loading dashboard...</span>\n        </div>\n      </div>\n    );\n  }\n\n  return (\n    <div className=\"space-y-6\">\n      {/* Header */}\n      <div className=\"flex items-center justify-between\">\n        <div>\n          <h1 className=\"text-3xl font-bold text-white\">Trading Dashboard</h1>\n          <p className=\"text-gray-400 mt-1\">\n            Last updated: {lastUpdate.toLocaleTimeString()}\n          </p>\n        </div>\n        <button\n          onClick={fetchDashboardData}\n          className=\"flex items-center space-x-2 bg-blue-600 hover:bg-blue-700 px-4 py-2 rounded-lg transition-colors\"\n        >\n          <Activity className=\"w-4 h-4\" />\n          <span>Refresh</span>\n        </button>\n      </div>\n\n      {/* Key Metrics Cards */}\n      <div className=\"grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6\">\n        {/* Portfolio Value */}\n        <div className=\"bg-gray-800 rounded-lg p-6 border border-gray-700\">\n          <div className=\"flex items-center justify-between\">\n            <div>\n              <p className=\"text-gray-400 text-sm font-medium\">Portfolio Value</p>\n              <p className=\"text-2xl font-bold text-white mt-1\">\n                {portfolioData ? formatCurrency(portfolioData.total_value) : '$0'}\n              </p>\n              {portfolioData && (\n                <p className={`text-sm mt-1 ${\n                  portfolioData.total_return_percent >= 0 ? 'text-green-400' : 'text-red-400'\n                }`}>\n                  {formatPercent(portfolioData.total_return_percent)} \n                  ({formatCurrency(portfolioData.total_return)})\n                </p>\n              )}\n            </div>\n            <div className=\"bg-blue-600 p-3 rounded-lg\">\n              <DollarSign className=\"w-6 h-6\" />\n            </div>\n          </div>\n        </div>\n\n        {/* Active Positions */}\n        <div className=\"bg-gray-800 rounded-lg p-6 border border-gray-700\">\n          <div className=\"flex items-center justify-between\">\n            <div>\n              <p className=\"text-gray-400 text-sm font-medium\">Active Positions</p>\n              <p className=\"text-2xl font-bold text-white mt-1\">\n                {portfolioData?.number_of_positions || 0}\n              </p>\n              <p className=\"text-gray-400 text-sm mt-1\">\n                Invested: {portfolioData ? formatCurrency(portfolioData.invested_amount) : '$0'}\n              </p>\n            </div>\n            <div className=\"bg-green-600 p-3 rounded-lg\">\n              <Target className=\"w-6 h-6\" />\n            </div>\n          </div>\n        </div>\n\n        {/* Win Rate */}\n        <div className=\"bg-gray-800 rounded-lg p-6 border border-gray-700\">\n          <div className=\"flex items-center justify-between\">\n            <div>\n              <p className=\"text-gray-400 text-sm font-medium\">Success Rate</p>\n              <p className=\"text-2xl font-bold text-white mt-1\">\n                {tradingStats ? `${tradingStats.success_rate.toFixed(1)}%` : '0%'}\n              </p>\n              <p className=\"text-gray-400 text-sm mt-1\">\n                {tradingStats ? `${tradingStats.successful_trades}/${tradingStats.total_signals} trades` : '0/0 trades'}\n              </p>\n            </div>\n            <div className=\"bg-purple-600 p-3 rounded-lg\">\n              <Brain className=\"w-6 h-6\" />\n            </div>\n          </div>\n        </div>\n\n        {/* Total Signals */}\n        <div className=\"bg-gray-800 rounded-lg p-6 border border-gray-700\">\n          <div className=\"flex items-center justify-between\">\n            <div>\n              <p className=\"text-gray-400 text-sm font-medium\">AI Signals</p>\n              <p className=\"text-2xl font-bold text-white mt-1\">\n                {tradingStats?.total_signals || 0}\n              </p>\n              <p className=\"text-gray-400 text-sm mt-1\">\n                Engine uptime: {tradingStats ? `${tradingStats.uptime_hours.toFixed(1)}h` : '0h'}\n              </p>\n            </div>\n            <div className=\"bg-yellow-600 p-3 rounded-lg\">\n              <Zap className=\"w-6 h-6\" />\n            </div>\n          </div>\n        </div>\n      </div>\n\n      {/* Charts Row */}\n      <div className=\"grid grid-cols-1 lg:grid-cols-2 gap-6\">\n        {/* Portfolio Performance Chart */}\n        <div className=\"bg-gray-800 rounded-lg p-6 border border-gray-700\">\n          <div className=\"flex items-center justify-between mb-4\">\n            <h3 className=\"text-lg font-semibold text-white\">Portfolio Performance</h3>\n            <BarChart3 className=\"w-5 h-5 text-gray-400\" />\n          </div>\n          <div className=\"h-64\">\n            <ResponsiveContainer width=\"100%\" height=\"100%\">\n              <AreaChart data={performanceData}>\n                <CartesianGrid strokeDasharray=\"3 3\" stroke=\"#374151\" />\n                <XAxis \n                  dataKey=\"date\" \n                  stroke=\"#9CA3AF\" \n                  fontSize={12}\n                  tickFormatter={(value) => new Date(value).toLocaleDateString()}\n                />\n                <YAxis stroke=\"#9CA3AF\" fontSize={12} />\n                <Tooltip \n                  contentStyle={{ \n                    backgroundColor: '#1F2937', \n                    border: '1px solid #374151',\n                    borderRadius: '8px'\n                  }}\n                  formatter={(value: number, name: string) => [\n                    name === 'value' ? formatCurrency(value) : formatCurrency(value),\n                    name === 'value' ? 'Portfolio Value' : 'P&L'\n                  ]}\n                />\n                <Area \n                  type=\"monotone\" \n                  dataKey=\"value\" \n                  stroke=\"#3B82F6\" \n                  fill=\"#3B82F6\" \n                  fillOpacity={0.3}\n                />\n              </AreaChart>\n            </ResponsiveContainer>\n          </div>\n        </div>\n\n        {/* Market Trends */}\n        <div className=\"bg-gray-800 rounded-lg p-6 border border-gray-700\">\n          <div className=\"flex items-center justify-between mb-4\">\n            <h3 className=\"text-lg font-semibold text-white\">Market Trends</h3>\n            <TrendingUp className=\"w-5 h-5 text-gray-400\" />\n          </div>\n          <div className=\"space-y-3\">\n            {marketData.length > 0 ? (\n              marketData.map((stock, index) => (\n                <div key={stock.symbol} className=\"flex items-center justify-between p-3 bg-gray-700 rounded-lg\">\n                  <div className=\"flex items-center space-x-3\">\n                    <div \n                      className=\"w-3 h-3 rounded-full\"\n                      style={{ backgroundColor: COLORS[index % COLORS.length] }}\n                    />\n                    <div>\n                      <p className=\"font-medium text-white\">{stock.symbol}</p>\n                      <p className=\"text-sm text-gray-400 truncate max-w-32\">\n                        {stock.company_name}\n                      </p>\n                    </div>\n                  </div>\n                  <div className=\"text-right\">\n                    <p className=\"font-medium text-white\">\n                      ${stock.current_price.toFixed(2)}\n                    </p>\n                    <p className={`text-sm ${\n                      stock.percent_change >= 0 ? 'text-green-400' : 'text-red-400'\n                    }`}>\n                      {formatPercent(stock.percent_change)}\n                    </p>\n                  </div>\n                </div>\n              ))\n            ) : (\n              <div className=\"text-center text-gray-400 py-8\">\n                <TrendingDown className=\"w-8 h-8 mx-auto mb-2\" />\n                <p>No market data available</p>\n              </div>\n            )}\n          </div>\n        </div>\n      </div>\n\n      {/* Alerts and Activity */}\n      <div className=\"grid grid-cols-1 lg:grid-cols-2 gap-6\">\n        {/* Recent Alerts */}\n        <div className=\"bg-gray-800 rounded-lg p-6 border border-gray-700\">\n          <div className=\"flex items-center justify-between mb-4\">\n            <h3 className=\"text-lg font-semibold text-white\">Recent Activity</h3>\n            <Activity className=\"w-5 h-5 text-gray-400\" />\n          </div>\n          <div className=\"space-y-3 max-h-64 overflow-y-auto\">\n            {alerts.length > 0 ? (\n              alerts.slice(0, 8).map((alert) => (\n                <div key={alert.id} className=\"flex items-start space-x-3 p-3 bg-gray-700 rounded-lg\">\n                  <div className=\"flex-shrink-0 mt-1\">\n                    {alert.type === 'TRADE_EXECUTED' ? (\n                      <CheckCircle className=\"w-4 h-4 text-green-400\" />\n                    ) : alert.type === 'ERROR' ? (\n                      <AlertTriangle className=\"w-4 h-4 text-red-400\" />\n                    ) : (\n                      <Clock className=\"w-4 h-4 text-blue-400\" />\n                    )}\n                  </div>\n                  <div className=\"flex-1 min-w-0\">\n                    <p className=\"text-sm text-white truncate\">{alert.message}</p>\n                    <p className=\"text-xs text-gray-400 mt-1\">\n                      {new Date(alert.timestamp).toLocaleTimeString()} \n                      {alert.symbol && ` • ${alert.symbol}`}\n                    </p>\n                  </div>\n                </div>\n              ))\n            ) : (\n              <div className=\"text-center text-gray-400 py-8\">\n                <Clock className=\"w-8 h-8 mx-auto mb-2\" />\n                <p>No recent activity</p>\n              </div>\n            )}\n          </div>\n        </div>\n\n        {/* Risk Metrics */}\n        <div className=\"bg-gray-800 rounded-lg p-6 border border-gray-700\">\n          <div className=\"flex items-center justify-between mb-4\">\n            <h3 className=\"text-lg font-semibold text-white\">Risk Metrics</h3>\n            <Shield className=\"w-5 h-5 text-gray-400\" />\n          </div>\n          <div className=\"space-y-4\">\n            {/* Cash Reserve */}\n            <div className=\"flex items-center justify-between\">\n              <span className=\"text-gray-400\">Cash Reserve</span>\n              <span className=\"text-white font-medium\">\n                {portfolioData ? formatCurrency(portfolioData.cash_balance) : '$0'}\n              </span>\n            </div>\n            \n            {/* Invested Amount */}\n            <div className=\"flex items-center justify-between\">\n              <span className=\"text-gray-400\">Total Invested</span>\n              <span className=\"text-white font-medium\">\n                {portfolioData ? formatCurrency(portfolioData.invested_amount) : '$0'}\n              </span>\n            </div>\n            \n            {/* Unrealized P&L */}\n            <div className=\"flex items-center justify-between\">\n              <span className=\"text-gray-400\">Unrealized P&L</span>\n              <span className={`font-medium ${\n                (portfolioData?.unrealized_pnl || 0) >= 0 ? 'text-green-400' : 'text-red-400'\n              }`}>\n                {portfolioData ? formatCurrency(portfolioData.unrealized_pnl) : '$0'}\n              </span>\n            </div>\n            \n            {/* Realized P&L */}\n            <div className=\"flex items-center justify-between\">\n              <span className=\"text-gray-400\">Realized P&L</span>\n              <span className={`font-medium ${\n                (portfolioData?.realized_pnl || 0) >= 0 ? 'text-green-400' : 'text-red-400'\n              }`}>\n                {portfolioData ? formatCurrency(portfolioData.realized_pnl) : '$0'}\n              </span>\n            </div>\n\n            {/* Risk Level Indicator */}\n            <div className=\"mt-4 p-3 bg-gray-700 rounded-lg\">\n              <div className=\"flex items-center justify-between mb-2\">\n                <span className=\"text-gray-400 text-sm\">Risk Level</span>\n                <span className=\"text-green-400 text-sm font-medium\">Conservative</span>\n              </div>\n              <div className=\"w-full bg-gray-600 rounded-full h-2\">\n                <div \n                  className=\"bg-green-400 h-2 rounded-full transition-all duration-300\"\n                  style={{ width: '25%' }}\n                />\n              </div>\n              <p className=\"text-xs text-gray-400 mt-2\">\n                Portfolio risk is well within safe limits\n              </p>\n            </div>\n          </div>\n        </div>\n      </div>\n\n      {/* Quick Actions */}\n      <div className=\"bg-gray-800 rounded-lg p-6 border border-gray-700\">\n        <h3 className=\"text-lg font-semibold text-white mb-4\">Quick Actions</h3>\n        <div className=\"grid grid-cols-2 md:grid-cols-4 gap-4\">\n          <button className=\"flex items-center justify-center space-x-2 bg-blue-600 hover:bg-blue-700 p-4 rounded-lg transition-colors\">\n            <Brain className=\"w-5 h-5\" />\n            <span>View Predictions</span>\n          </button>\n          \n          <button className=\"flex items-center justify-center space-x-2 bg-green-600 hover:bg-green-700 p-4 rounded-lg transition-colors\">\n            <Target className=\"w-5 h-5\" />\n            <span>Manual Trade</span>\n          </button>\n          \n          <button className=\"flex items-center justify-center space-x-2 bg-purple-600 hover:bg-purple-700 p-4 rounded-lg transition-colors\">\n            <PieChart className=\"w-5 h-5\" />\n            <span>Portfolio Details</span>\n          </button>\n          \n          <button className=\"flex items-center justify-center space-x-2 bg-yellow-600 hover:bg-yellow-700 p-4 rounded-lg transition-colors\">\n            <Activity className=\"w-5 h-5\" />\n            <span>Market Analysis</span>\n          </button>\n        </div>\n      </div>\n    </div>\n  );\n};\n\nexport default Dashboard;"
